(() => {
    enum AudioLevel {
        min,  //0
        medium, //1
        max //2
    } 

    const currentAudio = AudioLevel.medium

    console.log(currentAudio)
    console.log(AudioLevel)
}) ()